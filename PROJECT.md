# 프로젝트 상태

## 현재 상태

- 프로젝트: 항공권 특가 알림 앱 (날짜·목적지 안 가리고 평소보다 압도적으로 싼 항공권만 큐레이션해 알림. 출국의신류).
- 현재 단계: 1단계 - 데이터 확보·품질 검증.
- 목표: Travelpayouts Aviasales Data API(캐시 기반)에 한국발 10~20만원대 초특가가 실제로 잡히는지 확인.
- 6·7·8번(스토어/비용/멀티앱)은 한참 뒤 단계. 지금은 참고만 하고 1단계 작업에 집중.

---

# 앱 개발 프로젝트 지침

## 0. 작업 원칙 (Claude 동작 규칙)

- 요구사항이 불명확하면 코드 작성 전에 질문한다. 추측으로 진행하지 않는다.
- 항상 단계별로 사고한 뒤 코드를 쓴다.
- 단순하고 유지보수 가능한 해법을 복잡한 것보다 우선한다.
- 출력 형식: 짧은 설명 -> 코드 -> 개선·엣지케이스.
- 디버깅 시 근본 원인을 먼저 설명하고, 최소 수정안을 먼저 제시한 뒤 장기 개선안을 제안한다.
- 프로덕션에서 돌아간다고 가정하고 확장성·성능·보안을 항상 고려한다.

## 1. 개발 순서 (역순 금지)

- 데이터 소스를 먼저 확보하고 품질을 실제 데이터로 검증한다.
- 핵심 로직(서버)을 먼저 동작·검증한다.
- UI/앱 화면은 마지막에 붙인다.
- 각 단계는 "동작 확인됨" 체크포인트를 통과해야 다음으로 간다. 전체를 한 번에 만들지 않는다.
- 외부 데이터 의존 앱은 이 순서를 어기면 안 된다. 데이터가 없으면 앱도 없다.

## 2. 데이터 소스 정책

- 공식 API 또는 공식 제휴 프로그램만 사용한다. 스크래핑, 비공식 엔드포인트, ToS 위반은 금지한다.
- 신규 데이터 소스는 사용 전 다음을 반드시 확인한다: 무료/유료 경계, 레이트리밋, 데이터 신선도(실시간 vs 캐시), 보관 기간, 상업적 사용 허용 여부, 출처 표기 의무.
- 데이터가 캐시·지연·부분 커버리지면 그 한계를 설계에 반영하고 사용자에게 과장하지 않는다.
- 신규 데이터 소스 탐색 시 참조: public-apis/public-apis, public-api-lists, 한국 서비스용 Public API 모음.

## 3. 기본 기술 스택 (혼자 / 무예산 / 모바일 우선)

- 앱: Expo (React Native) - iOS·Android 단일 코드.
- 백엔드: Python 또는 Node, 인프로세스 스케줄러, 단일 인스턴스로 시작.
- DB: 무료 티어 Postgres 또는 SQLite로 시작.
- 푸시: Expo Push로 시작, 규모 확대 시 FCM 직접 전환.
- 호스팅: 무료~최소 비용 단일 인스턴스로 시작.
- 스택은 검증 후에만 복잡도를 올린다. 처음부터 마이크로서비스·큐·캐시 레이어를 두지 않는다.

## 4. 코드 원칙

- 핵심 비즈니스 로직은 외부 의존성 없는 순수 함수로 분리해 단위 테스트·튜닝이 가능하게 한다.
- 판정·통계·점수 로직은 진단값을 항상 함께 반환해 사후 백테스트·임계값 튜닝이 가능하게 한다.
- 소표본으로 결론을 내지 않는다. 최소 표본 수·워밍업 기간 게이트를 알림/판정 전 필수 조건으로 둔다.
- 임계값 등 튜닝 파라미터는 한 곳에 모으고, 코드 곳곳에 흩지 않는다.
- 외부 호출·알림 경로에는 멱등성, 중복 억제, 레이트리밋, 재시도 백오프를 기본 적용한다.
- 비동기·효율 패턴을 가능한 곳에 적용한다. 불필요한 의존성은 추가하지 않는다.
- 모듈화·재사용 가능하게 작성하고, 중요한 로직에만 간결한 주석을 단다.

## 5. 보안

- 시크릿(API 토큰, 키, 자격증명)은 환경변수 또는 시크릿 매니저로만 관리한다. 클라이언트 번들·저장소·로그에 절대 포함하지 않는다.
- 클라이언트가 보내는 식별자(예: device_id)를 신뢰하지 않는다. 서버 발급 토큰으로 소유권을 검증한다.
- 공개 API에 인증과 레이트리밋을 둔다. 인증 없는 쓰기 엔드포인트를 프로덕션에 두지 않는다.
- 사용자 입력은 항상 검증한다. SQL은 파라미터 바인딩만 사용한다.

## 6. 스토어 출시 필수 (iOS·Android 공통)

- 개인정보 처리방침을 작성하고, 앱 내 링크가 실제로 작동해야 한다.
- 애플 App Privacy 라벨 / 구글 Data Safety 양식을 실제 수집 항목과 정확히 일치하게 작성한다. 불일치 시 거절된다. 2026년 기준 개인정보 공시가 강화되었다.
- 푸시 알림은 사용자 동의 후에만 보낸다. 마케팅성 스팸 알림 금지.
- 미완성 화면, 플레이스홀더 텍스트, 깨진 링크가 없어야 한다. 첫 제출의 약 17%가 이런 사전 방지 가능한 사유로 거절된다.
- 단순 웹뷰 래퍼 금지. 네이티브 내비게이션·푸시·오프라인 처리 등 앱다운 요소를 갖춘다 (가이드라인 4.2).
- 포화 카테고리 앱은 명확한 차별점이 있어야 하고, 심사 메모에 그 차별점을 설명한다 (가이드라인 4.3 스팸).
- 인앱결제로 디지털 상품을 팔 때만 스토어 수수료가 발생한다. 무료 앱 + 외부 제휴 수익 모델은 스토어 수수료 0이다.
- 메타데이터(앱 이름·설명·스크린샷)는 앱의 실제 기능을 정확히 반영해야 한다. 과장·오해 소지 시 거절된다.

## 7. 비용·계정 (실비, 회피 불가)

- Apple Developer Program: 연 99 USD, 매년 갱신. 개인 가입 시 본인 명의 카드 필요.
- Google Play Console: 최초 1회 25 USD, 연 갱신비 없음.
- 계정 승인에도 시간이 걸리므로 개발과 병행해 초기에 가입한다.
- 데이터 제휴 가입(예: Travelpayouts)은 무료지만 토큰 발급·연결에 시간이 든다. 1단계에서 먼저 처리한다.

## 8. 멀티 앱 운영 (앱을 여러 개 만들 때)

- 공통 로직(인증, 푸시, 스케줄러, 데이터 클라이언트, 알림 팬아웃)은 재사용 가능한 모듈로 분리해 앱마다 재구현하지 않는다.
- 앱별 차별화는 비즈니스 로직(판정·큐레이션·UX)에 둔다. 인프라는 공유한다.
- 동일 개발자 계정으로 여러 무가치·유사 앱을 양산하지 않는다. 스팸으로 간주되면 계정 전체가 제거될 수 있다. 각 앱은 독립적 가치를 가져야 한다.
- 각 앱은 데이터 소스·수익 모델·차별점을 출시 전에 한 줄로 정의할 수 있어야 한다. 정의 안 되면 만들지 않는다.

## 9. 검증 우선순위

- 커버리지보다 거짓 양성률(잘못된 알림·결과)을 먼저 잡는다. 사용자 이탈의 직접 원인은 후자다.
- 실데이터 없이도 합성 데이터 + 단위 테스트로 핵심 로직 임계값을 사전 캘리브레이션한다.
- "쓸 만한지"는 본인이 직접 며칠 써보고 판단한다. 데이터 품질이 안 받쳐주면 그 앱은 만들지 않는다.
